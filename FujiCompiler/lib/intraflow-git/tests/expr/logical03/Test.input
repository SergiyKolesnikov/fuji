
class Test {

	void m(int a, int b, int c) {
		
		boolean r;
		
		r = !(a>0) && !(b>0) || c>0 && d>0;
		
		r = !(a>0 && c<0);
		
		r = r && (a>0 || c<0);
		
		f(a>0 && c<0, a>0 || c<0);
		
		b = a>0 && c<0 ? 
			a>1 || c<1 : 
			a>2 && c<2;
			
		boolean[][] array = 
			{
				{ a>0 && c<0, a>0 || c<0 } ,
			 	{ a>5 && c<6 }
			};
	}
	
	void f(boolean x, boolean y) { }
}