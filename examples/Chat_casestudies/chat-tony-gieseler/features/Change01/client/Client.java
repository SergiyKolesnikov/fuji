package client;

import common.Change01;

/**
 * TODO description
 */
public class Client {
	private void start(String host, int port) {
		Change01 v = new Change01();
		pass = v.crypt(pass);
		
		original(host, port);
	}
}