public refines  class ChatClient {
	
	
		/**
	 * Constructs a ChatClient and lays a GUI over it.
	 * @param args
	 */
	public static void main(String[] args) {
		ChatClient cc= new ChatClient();
		ChatClientConsole ccc= new ChatClientConsole(cc);
	}

}